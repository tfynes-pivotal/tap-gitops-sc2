apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: harbor-cert
  namespace: harbor
spec:
  dnsNames:
  - myrepo-harbor.akslab4.tap.fynesy.com
  secretName: harbor-tls-cert
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer